<%

include("util/constants.jag");
var log = new Log();


function authenticate(){

 if(session.get('user') == null){
            throw 'InvalidLogin';
       }
 if(session.get('user') != request.getParameter("currentLoggedUser")){
     print('{"success":false, "message":"Invalid user in the session."}');
        }
}

authenticate();

%>
